---
- name: reto 1 ansible
  hosts: web
  become: yes
  gather_facts: no
  
  tasks:

  - name: Get hostname
    ansible.windows.win.command:
      cmd: echo $HOSTNAME
    register: hostname
    
  - ansible.builtin.debug:
      msg: The hostname is {{ hostname }}
